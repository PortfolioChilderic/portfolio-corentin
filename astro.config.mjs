import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://portfoliochilderic.github.io',
  base: '/portfolio-corentin/',
  output: 'static',
  trailingSlash: 'ignore',
});